import Foundation
import WidgetKit

// Navigation intents are disabled for now to avoid AppIntentsSSUTraining build errors.
// To re-enable interactive navigation buttons in the widgets, uncomment the code below
// and add `import AppIntents` at the top.

// These will be re-enabled once the project is building successfully.
